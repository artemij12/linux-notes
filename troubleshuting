если установка и обновление пакетов было прервано, то появляется ошибка dpkg was interrupted, при этом необохдимо выполнить команду
sudo dpkg --configure -a
в случае если не помогло, то необходимо заново построить зависимости
sudo apt --fix-broken install
